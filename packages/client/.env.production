# Production — container server deployed via Azure Container Apps.
# After `terraform apply`, run: terraform output container_app_url
# Paste the result here and rebuild the client before deploying to SWA.
VITE_API_URL=https://baby-tracker-app-container.azurecontainerapps.io
