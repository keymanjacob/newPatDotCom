# Required
DATABASE_URL=postgres://user:password@ep-name.region.aws.neon.tech/neondb?sslmode=require

# Optional — defaults shown
PORT=3001
CORS_ORIGINS=http://localhost:5173

# Optional — omit to disable realtime; falls back to polling-only mode
AZURE_WEB_PUBSUB_CONNECTION_STRING=
